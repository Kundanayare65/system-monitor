[package]
name = "system-monitor"
version = "0.1.0"
edition = "2021"

[dependencies]
sysinfo = "0.30"
axum = "0.7"
tokio = { version = "1", features = ["full"] }
serde_json = "1"
tower-http = { version = "0.6", features = ["cors"] }